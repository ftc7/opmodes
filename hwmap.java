package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.HardwareDevice;
import com.qualcomm.robotcore.util.Hardware;
import com.qualcomm.robotcore.hardware.HardwareMap;
import com.qualcomm.robotcore.hardware.DistanceSensor;
import com.qualcomm.robotcore.hardware.ColorSensor;

public class hwmap {
    /*ColorSensor colorSense;
    DcMotor frontLeft;
    DcMotor frontRight;
    DcMotor backLeft;
    DcMotor backRight;
    DistanceSensor distanceSense;
    
    HardwareMap map;

    public void init(HardwareMap initmap) {
        map = initmap;
        ColorSensor colorSense = map.colorSensor.get("sensor_color_distance");
        DcMotor frontLeft = map.get(DcMotor.class, "FrontLeft");
        DcMotor frontRight = map.get(DcMotor.class, "frontRight");
        DcMotor backLeft = map.get(DcMotor.class, "backLeft");
        DcMotor backRight = map.get(DcMotor.class, "backRight");
        DistanceSensor distanceSense = map.get(DistanceSensor.class, "sensor_color_distance");
    
        frontLeft.setMode(DcMotor.RunMode.RUN_USING_ENCODER);
        frontRight.setMode(DcMotor.RunMode.RUN_USING_ENCODER);
        backLeft.setMode(DcMotor.RunMode.RUN_USING_ENCODER);
        backRight.setMode(DcMotor.RunMode.RUN_USING_ENCODER);
        frontLeft.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        frontRight.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        backLeft.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        backRight.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
    
        frontLeft.setDirection(DcMotor.Direction.REVERSE);
        backLeft.setDirection(DcMotor.Direction.REVERSE);
        int red = colorSense.green();
    }*/
}
